#include<stdio.h>
#include<string.h>
void main()
{
char name[200];
int length;
printf("enter the length of the string");
scanf("%d",&length);
length=strlen(name);
printf("\n the length of the string is",length);
}
